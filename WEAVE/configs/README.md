# Retired Configuration Directory

There are no active JSON configurations in this directory.

Use the two canonical files at the project root:

- `config.json` for model, bridge, training, data, and checkpoint settings.
- `inference.json` for inference and evaluation settings.

Historical experiment configurations were moved to `archives/legacy-configs-20260715/`. They are provenance only and must not be used as submission entry points.
