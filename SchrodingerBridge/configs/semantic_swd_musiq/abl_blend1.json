{
  "_base": "swd_cm_sem_r8.json",
  "bridge": {
    "swd_semantic_mode": "region",
    "swd_semantic_regions": 8,
    "swd_semantic_blend": 1.0,
    "dwt_route_train_prob": 0.8
  },
  "checkpoint": {
    "save_dir": "./exp/abl_blend1"
  },
  "ablation": {
    "name": "abl_blend1",
    "axis": "blend_sweep",
    "stage": "ablation",
    "notes": "blend=1.0: pure global SWD (no region contribution). Contrast with blend=0.7 (optimal) and blend=0 (pure region). Expect MUSIQ in between pure global and optimal."
  }
}