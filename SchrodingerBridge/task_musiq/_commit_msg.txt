refactor(swd): delete dead k-means semantic SWD code, add DWT-energy guidance

- Delete _kmeans_labels, _semantic_region_swd, _semantic_patch_swd, _semantic_band_swd
  (confirmed ineffective: S1-S4 all failed to beat baseline MUSIQ=41.11 significantly)
- Remove 3 semantic dispatch branches from _compute_swd (region_patch/region_band/region)
- Remove swd_semantic_* attributes from __init__
- Add _dwt_energy_weight: content-adaptive SWD weight from DWT high-freq energy
- Add combined cross_attn_plus_dwt guidance mode (element-wise product, renormalized)
- Pass content param to _cross_attn_swd_weight for DWT-energy and combined modes
- Add S5 (band-split + cross-attn-guided), S6 (multi-patch + cross-attn-guided),
  S7 (DWT-energy guided), S8 (combined) configs
- Add eval_only.py for single-checkpoint evaluation
- Update state files with S1-S4 results and key insight

Remaining loops are fixed-size flat (4 DWT bands, 3 patch sizes), fully batch-parallel.
