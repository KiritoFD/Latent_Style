# 2026-07-07

- Created a system architecture diagram for the SchrodingerBridge (LBM latent style transfer) project.
  - Reflects CURRENT code state: legacy `TimeConditionedLANCETBridge` was removed in 630 Phase 1C; active contracts are `620_spectral_ode` (spectral_bridge620) and `620_spatial_bridge` (model620). Core path: frozen VAE encode → style encoder (DINO) → time-conditioned U-Net backbone (blocks620) → Haar DWT freq decomposition → ODE integrator (Euler/Heun/RK4) → style injection (AdaIN/Cross-attn/Tone bias) → VAE decode. Training objectives: Flow matching, Terminal SWD, Kinetic, Endpoint (content+style).
  - Deliverable: `docs/architecture_diagram.svg` (also shown inline via Visualizer).
  - Note: `docs/archive/architecture.md` is STALE (describes LANCET bridge) — do not trust it for current architecture.
