# attn_gw_ot Closure

- Status: pending
- Current closure path:
  - remote train is active
  - local fast eval will auto-start after `SaMAM` finishes
  - local stage-close packet will auto-start after fast convergence
