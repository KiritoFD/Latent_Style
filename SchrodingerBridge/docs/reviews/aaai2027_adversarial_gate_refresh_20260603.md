# AAAI 2027 Adversarial Gate Refresh

Date: 2026-06-03  
Lane: `adversarial_review`  
Scope: current paper + working index + goal direction + latest tokenizer-localization packet surface

## 1) Top 5 weak-reject risks right now

1. **Tokenizer mechanism closure is still only partial.**  
   The landed `L`-family execution-alignment packet narrowed the story, but it still does not localize whether the remaining ceiling is primarily style-branch weakness or executor-side attenuation.

2. **Efficiency remains a bounded timing-context story, not a fair parity result.**  
   The paper is now much safer on wording, but Gate C still does not support a comparative normalized time-to-parity claim.

3. **Distinct5 remains a narrow metric-stress benchmark with proxy-based quality evidence.**  
   This is paper-safe only under tight scope; it remains a weak-reject driver if the paper tries to read it as broad AST superiority.

4. **The paper still lacks a strong positive mechanism differentiator.**  
   Gate A closed negatively, Gate B closed negatively, and the current tokenizer evidence is still a bounded partial diagnosis rather than a decisive positive mechanism win.

5. **Theory-to-evidence closure is still incomplete.**  
   The current paper has narrowed its mathematical rhetoric, but the stronger latent-metric and path-stability story still depends on planned or incomplete supporting packets rather than fully landed closure evidence.

## 2) Exact evidence gaps

### Risk 1: tokenizer mechanism closure

Missing evidence:

- the paired tokenizer-localization run itself is not yet landed;
- no matched `fresh style branch / frozen executor` vs `frozen style branch / fresh executor` result exists yet on the current Distinct5 `L e1` surface;
- therefore the paper still cannot say whether most recoverable gain comes from fresher control or fresher execution.

### Risk 2: efficiency / parity

Missing evidence:

- no matched same-scope SaMST curve exists;
- current Distinct5 timing packet still mixes:
  - `LBM` operating points,
  - `SaMAM` partial curve,
  - `SaMST` operating point only;
- therefore there is still no fair time-to-threshold or time-to-parity closure.

### Risk 3: Distinct5 scope / perceptual validity

Missing evidence:

- no human study or rubric-style preference closure;
- no broader dataset replication beyond the current Distinct5 metric-stress framing that would justify stronger external validity.

### Risk 4: positive mechanism differentiator

Missing evidence:

- no currently landed packet gives a clean positive closure analogous to:
  - `this mechanism wins clearly over its nearest matched alternative`;
- instead:
  - endpoint-only pointwise supervision closed negatively,
  - semantic-vs-random SA-SWD closed negatively,
  - tokenizer execution-alignment is only partially diagnostic.

### Risk 5: theory-to-evidence closure

Missing evidence:

- the planned tokenizer-localization packet is still pending;
- the path-stability packet is still planned;
- the stronger latent-metric narrative still has only bounded support rather than a full closed empirical chain.

## 3) Ranked experiment / doc / code cleanup queue

1. **Experiment:** land the Distinct5 `L e1` tokenizer-localization pair exactly as specified, then run a bounded three-lane review on the result.  
   This is the highest-value next closure because it attacks the current top weak-reject driver directly.

2. **Doc / manuscript boundary update:** once that pair lands, update the review registry, working index, and manuscript claim boundary so the tokenizer mechanism story reflects the new localization outcome cleanly.  
   Do not expand claims before that review pass.

3. **Experiment:** decide Gate C strategically:
   - either freeze the paper permanently at bounded timing-context wording,
   - or pay for a stronger matched efficiency packet with comparable curves.

4. **Experiment:** land the path-stability / theory-support packet only if the paper still wants stronger mathematical-design rhetoric beyond the current bounded version.

5. **Code / surface cleanup:** prune or clearly demote superseded tokenizer probe surfaces (`blocked H packet`, legacy256 localization preflight, stale fallback notes) so the paper-facing experiment tree shows one live tokenizer-localization line instead of several near-duplicates.
