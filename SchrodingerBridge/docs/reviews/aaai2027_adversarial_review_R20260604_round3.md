# AAAI 2027 adversarial review round 3

Date: 2026-06-04

Scope:
- Main draft: `SchrodingerBridge/aaai_submission/paper_aaai2026.tex`
- Current PDF: `SchrodingerBridge/aaai_submission/paper_aaai2026.pdf`
- Reviewer inputs: Kant, Faraday, Wegener, Cicero

## Applied in this writing pass

- Rewrote the abstract and introduction around IDT as an evaluator-relative falsification contract, not a universal definition of style transfer.
- Removed the abstract-level hours-vs-minutes comparison phrase and kept the cost claim as selected-checkpoint footprint.
- Reworded SaMAM claims from "improves ArtFID" to "lowers the targetwise ArtFID diagnostic" and kept SaMAM as current reproduced checkpoint estimates until Dalton returns a complete paired packet.
- Bounded bridge language: LBM borrows flow/OT vocabulary only as implementation language for deterministic endpoint editing.
- Changed terminal loss notation from `SA-SWD` as a standard-looking distance to `sem-proj`, and renamed the method text to semantic-axis sorted-projection regularization.
- Marked the bootstrap intervals as exploratory row-paired intervals rather than broad statistical superiority evidence.
- Reframed kinetic language as velocity-magnitude regularization and measured latent-motion association, not proof of unnecessary-motion removal.
- Made tokenizer conclusions causal-safe: current evidence prioritizes factorization/execution budget over scaling, while fixed-tokenizer/fixed-executor swaps remain required for attribution.
- Simplified the Distinct5 qualitative panel by removing per-cell tiny metrics and enlarging the aligned transfer images.
- Removed the historical strict-750 full grid from the main paper and kept it as an audit artifact, so it no longer dominates the Distinct5 main evidence or crowds the representation table.
- Rebuilt the PDF and removed the checklist orphan page.

## Current PDF check

- Build command: `cmd /c build_paper.bat`
- Output: `SchrodingerBridge/aaai_submission/paper_aaai2026.pdf`
- Page count: 11
- Log scan: no undefined references/citations, no fatal errors, no overfull boxes found by the checked patterns.
- Font check: no Type 3 fonts; only XeLaTeX template font-substitution warnings remain.

## Remaining acceptance risks

- Distinct5-512 is still one fixed CLIP-separated WikiArt stress split. The writing now states this boundary, but stronger generality needs at least two additional fixed-rule splits.
- SaMAM remains a point-estimate curve until Dalton returns final/tuned checkpoint metrics, IDT-aligned per-image rows, targetwise ArtFID, and same-scope timing.
- Current bootstrap intervals are row-paired and exploratory; clustered bootstrap remains required for stronger statistical language.
- The full strict-750 grid is now out of the main paper. If needed later, it should return only as supplemental/audit material, not as a primary visual.
