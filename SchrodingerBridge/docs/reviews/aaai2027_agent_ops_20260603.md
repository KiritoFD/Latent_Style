# AAAI 2027 Agent Ops - 2026-06-03

This file is the current control-plane truth for the live subagents in this
thread. It replaces the earlier append-only heartbeat notes with one clean
roster and one set of standing rules.

## Current roster

| agent | id | state | owned surface | latest verified artifact | next trigger |
| --- | --- | --- | --- | --- | --- |
| `Linnaeus` | `019e897a-7844-7451-a041-925a3a577ea1` | `active` | remote RTX 3060 execution only | `I:\Github\Latent_Style_TokenizerClean\SchrodingerBridge\exp\aaai2027_tokenizer_localization_l_e1_executoronly_seed42_b44\remote_train.log` | executor-only `full_eval/epoch_0001..0003/summary.json` landing |
| `Darwin` | `019e898d-e154-7540-be8a-300e858034c7` | `parked_by_design` | `docs/reviews/` theory and claim-boundary memos only | `G:/GitHub/Latent_Style/SchrodingerBridge/docs/reviews/tokenizer_localization_claim_boundary_20260603.md` | tokenizer-localization matched pair lands, or manuscript tries to widen tokenizer / latent-metric claims |
| `Carver` | `019e89b6-2800-75d2-9b03-ef50ea26b82a` | `parked_by_design` | `docs/reviews/` adversarial review only | `G:/GitHub/Latent_Style/SchrodingerBridge/docs/reviews/aaai2027_adversarial_gate_refresh_20260603.md` | Distinct5 `L e1` localization pair with both arms fully evaluated |
| `Noether` | `019e8a5f-776c-7453-b53d-eae0a4f09682` | `parked_by_design` | `docs/references/` literature and related-work memos only | `G:/GitHub/Latent_Style/SchrodingerBridge/docs/references/tokenizer_representation_related_work_refresh_20260603.md` | concrete related-work gap, intro-framing gap, or new baseline family |

## Current live remote task

`Linnaeus` remains the sole remote owner and is currently attached to the
Distinct5 tokenizer-localization packet.

- packet note:
  - `G:/GitHub/Latent_Style/SchrodingerBridge/docs/experiments/2026-06-03-tokenizer-localization-remote-preflight.md`
- current run shape:
  - style-branch arm completed training and recovered full eval through remote Windows `py -3`
  - executor-only arm completed training, but its auto full-eval crashed and is now in recovery on the same remote machine
- reporting policy:
  - milestone only
  - preserve logs and exact artifact paths
  - no unrelated remote jobs

## 2026-06-03 audit snapshot

All four named agents replied to an explicit audit prompt in this thread.

- `Linnaeus` is active and attached to the remote Distinct5 `L e1` localization
  packet.
- `Darwin`, `Carver`, and `Noether` are not missing; they are intentionally
  parked and waiting on bounded evidence-trigger conditions.
- The previous confusion came from stale control-plane wording, not from agent
  disappearance.

## Standing orders

### `Linnaeus`

- exclusive owner of remote RTX 3060 execution
- may launch, monitor, recover, and collect remote evidence
- must not edit manuscript body or local code
- should report only:
  - training completion
  - full-eval start
  - `summary.json` landed
  - true stall or crash with exact paths
- current assignment:
  - keep watching `aaai2027_tokenizer_localization_l_e1_executoronly_seed42_b44`
    until `full_eval/epoch_0001..0003/summary.json` all land
  - current live blocker:
    - executor-only `epoch_0001` full-eval crashed on the remote eval
      surface with `ImportError: CLIPModel requires the PyTorch library but it
      was not found in your environment.`
  - active recovery path:
    - recover full eval on the same remote machine, preferring the previously
      validated Windows `py -3` route if the WSL eval environment remains
      incompatible
  - latest verified progress:
    - executor-only `full_eval/epoch_0001/summary.json` has landed
    - executor-only `full_eval/epoch_0002/summary.json` has landed
    - recovery has advanced into `epoch_0003`
    - keep the lane active until `epoch_0003` summary lands

### `Darwin`

- trigger-based standby
- theory owner for tokenizer, representation, and latent-distance claim
  boundaries
- may write only bounded memos under `docs/reviews/`
- must not edit manuscript body, code, or experiment artifacts
- current parked condition:
  - do nothing until the matched localization pair lands or a claim boundary is
    about to widen

### `Carver`

- trigger-based standby
- standing adversarial reviewer and weak-reject gatekeeper
- may write only bounded memos under `docs/reviews/`
- must not edit manuscript body, code, or experiment artifacts
- current parked condition:
  - do nothing until the matched localization pair lands and needs an
    adversarial read

### `Noether`

- trigger-based standby
- literature and related-work coverage owner
- may write only bounded memos under `docs/references/`
- must not edit manuscript body, code, or experiment artifacts
- current parked condition:
  - do nothing until a real related-work or intro-framing gap is identified

## Operating rules

1. Only `Linnaeus` owns live remote experiment execution.
2. Only the main thread may integrate cross-lane changes into code or the
   manuscript.
3. `Darwin`, `Carver`, and `Noether` stay alive but parked; they do not write
   unless explicitly reactivated for one bounded deliverable.
4. Only one side lane should be actively writing at a time unless write scopes
   are clearly disjoint.
5. New formal remote runs must follow:
   - `G:/GitHub/Latent_Style/SchrodingerBridge/docs/experiments/aaai2027_experiment_logging_contract_20260603.md`

## Audit result

After the forced heartbeat audit and reassignment:

- no agent is missing or abandoned;
- one agent is actively attached to the remote experiment lane;
- three agents are intentionally parked in trigger-based standby with explicit
  wake conditions;
- the control problem was documentation drift, not agent disappearance;
- only `Linnaeus` is allowed to touch the remote 3060 until the current packet
  closes.
