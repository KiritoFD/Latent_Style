# AAAI 2027 L-Family Post-Landing TeX Reread

Date: 2026-06-03  
Lane: `adversarial_review`  
Scope: manuscript reread after the landed `L`-family tokenizer execution-alignment successor packet

## 1) Weak-reject drivers that remain

1. **Tokenizer mechanism closure is still only partial.**  
   The landed `L`-family packet supports a narrow executed-style-survival reading, but it does not restore `H`-family continuity and does not isolate a renderer-only bottleneck theorem.

2. **Efficiency remains bounded timing context, not normalized parity.**  
   The current paper is safer than before, but Gate C still does not support a fair comparative time-to-parity claim.

3. **The manuscript is now stale relative to the landed tokenizer packet.**  
   Several tokenizer paragraphs still read as if the code-to-execution probe is only future work, which is now a reviewer-visible inconsistency.

4. **Distinct5 remains a narrow metric-stress split with proxy-based quality evidence.**  
   This is still fine if tightly scoped, but it remains a weak-reject driver if any language drifts toward broader AST superiority.

## 2) Exact paper claims / sentences / sections that must tighten now

### A. Discussion tokenizer paragraph must be updated

Current sentence:

- `... while still leaving a code-to-execution alignment probe as the next clean mechanism test.`

Problem:

- this is no longer true; the `L`-family successor packet has landed.

Required tightening:

- replace the future-test wording with a landed-packet boundary:
  - the landed `L`-family successor probe supports executed-style-survival as the sharper mechanism question,
  - but does not restore `H`-family continuity or rule out tokenizer-side weakness.

### B. Conclusion tokenizer sentence is still too broad

Current sentence:

- `Within the tested Distinct5 tokenizer variants, current probes show that raw code enlargement is insufficient and that executed style survival through a content-conditioned renderer is the sharper mechanism question.`

Problem:

- after the landed `L`-family packet, this still reads too close to a broad family-level theorem;
- it does not say that the strongest post-landing evidence comes from a payload-backed `L`-family successor packet rather than the blocked `H` packet.

Required tightening:

- add explicit packet scope:
  - `including a landed L-family successor probe, current evidence suggests ...`
- and keep the non-closure:
  - `... without ruling out tokenizer-side weakness outright.`

### C. Related-work / tokenizer framing should avoid paper-wide closure language

Current sentence:

- `our current probes suggest that executed style control, rather than tokenizer size by itself, is the sharper diagnosis target ...`

Problem:

- mostly safe, but after the landed packet it should not sound like all tokenizer-side uncertainty is settled.

Required tightening:

- keep it bounded to:
  - `current probes, including a landed L-family successor packet, suggest ...`
- and avoid any wording that reads like:
  - `tokenizer size no longer matters`
  - or
  - `renderer-side attenuation is now proven dominant`

## 3) Next highest-value action: manuscript edit or new experiment?

**Manuscript edit is the next highest-value action.**

Reason:

- the main immediate risk is now stale or slightly over-broad wording relative to already-landed evidence;
- the paper should first absorb the `L`-family packet with the narrow boundary already established in the review memos;
- only after that should new experiments be considered for raising the score further.
