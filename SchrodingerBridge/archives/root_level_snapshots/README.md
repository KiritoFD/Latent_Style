# Root-Level Snapshots Archive

This folder stores large packaged snapshots that were previously cluttering the `SchrodingerBridge/` root.

Archived here on 2026-05-16:

- `grid_search_3epoch.rar`
- `review_additional_experiments.rar`
- `src.zip`

These files were moved rather than deleted so the historical bundles remain recoverable without obscuring the active project surface.
