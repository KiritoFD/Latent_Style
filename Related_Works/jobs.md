2. 古典派单步基线 (Classic 1-Step): CUT (ECCV 2020)复现理由： 轻量级单向翻译的绝对基石，提供 PatchNCE 的消融对比。操作方法： 使用原作者官方 PyTorch 代码 (GitHub: taesungp/contrastive-unpaired-translation)，极易配置。使用你的 Photo <-> Monet 数据集在默认超参下训练 200 epochs。预期评估： 速度极快，结构保留尚可，但风格化程度（CLIP Score）受限于像素级对抗损失的瓶颈。3. 原生扩散基线 (Multi-step SDE): SDEdit (ICLR 2022)复现理由： 展示“去噪困境（Denoising Dilemma）”，作为所有基于 SD 图生图的下限与痛点论据。操作方法： 无需训练。调用 HuggingFace diffusers 库的 StableDiffusionImg2ImgPipeline，加载 runwayml/stable-diffusion-v1-5。关键设置： num_inference_steps=50。分别使用 strength=0.3 (弱) 和 strength=0.8 (强) 遍历测试集。预期评估： 0.3 时 LPIPS 好但 CLIP 差；0.8 时 CLIP 好但 LPIPS 崩塌。FPS 极低 (<1)。4. 前沿潜在流匹配基线 (SOTA Flow): LBM (ICCV 2025)复现理由： 代表 2025 年将薛定谔桥/流匹配引入 VAE 潜空间的最新顶会成果 。操作方法： 官方代码已开源 (github.com/gojasper/LBM) 。该仓库提供了极其友好的 diffusers 接口和预训练好的 HuggingFace 权重 。关键设置： 直接下载预训练 Checkpoint 进行 Zero-shot 推理，设置 NFE=1 验证其单步性能。预期评估： 质量极高，但即使是 NFE=1，由于需要完整加载扩散 U-Net，其 Parameter Size (GB级别) 和显存开销将成为其绝对劣势。5. 极致蒸馏基线 (SOTA Distillation): SD-Turbo (2024)复现理由： 极具统治力的单步对抗扩散蒸馏工作，代表着“重型底模强行单步”的最强对手 。操作方法： 无需训练。直接调用 diffusers 的 AutoPipelineForImage2Image 加载 stabilityai/sdxl-turbo。关键设置： 设置 num_inference_steps=1，guidance_scale=0.0。预期评估： 1 步出图，质量和速度双高。你将用数据证明：在特定风格（如梵高/莫奈）的边缘锐利度和保真度上，《LatentAdaCUT》凭借 9.8MB 的体积，达到了与这个数 GB 庞然大物相同的 Pareto 前沿。
