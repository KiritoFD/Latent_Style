# exp_splash_status.md

> 生成时间: 2026-04-09  
> 用途: 记录 `splash` / `46_splash` 当前能确认到的状态，避免把“没有 summary.json”误写成“没有做实验”

---

## 1. 当前确认到的事实

### `46_splash`

已确认存在：

- `epoch_0040.pt`
- `full_eval/`
- `tokenizer_distill/`
- 大量生成图片

### `splash`

已确认存在：

- `epoch_0040.pt`
- `full_eval/`
- `tokenizer_distill/`
- 大量生成图片

---

## 2. 当前缺的不是实验本身，而是汇总结果

目前没有直接抓到：

- `summary.json`

这意味着这条线不是没做，而是当前至少存在下面几种可能：

1. 评估脚本输出了图片，但没有生成汇总 json
2. 汇总文件在别的位置，还没被纳入当前检索
3. 这条线的评估流程中断在“图片已导出，但指标未汇总”的阶段

---

## 3. 为什么这篇状态文档要保留

在长期考古里，一个很常见的误判是：

- 查不到 `summary.json`
- 就以为实验没跑

但 `splash` 这条线已经说明事情没这么简单。  
它至少已经完成了：

- 训练
- checkpoint 保存
- 图片级评估输出

所以它应该被记成：

**已执行到可视化评估阶段，但当前缺统一指标汇总。**

---

## 4. 后续该怎么继续追

下一步如果继续深挖 `splash`，最值得做的是：

1. 在对应目录下继续找是否有 csv / html / 其它汇总文件
2. 对照 `batch_distill_full_eval.py` 的输出逻辑，检查它在什么条件下会只产图不产 `summary.json`
3. 如果需要，把 `splash` 作为“有结果图但无汇总指标”的特殊案例单独保留

---

## 5. 当前结论

`splash` 不是空目录。  
它是一个**已经跑到相当后面，但当前证据链还没闭环**的实验支线，因此值得单独记状态，而不是从文档里直接消失。

