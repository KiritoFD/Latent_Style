# 未完全坐实的问题与后续恢复建议

## 1. 目前已经比较稳的部分

- `ae596d1 -> d916277 -> 83ffe10 -> c619fda -> c8577e0 -> 426ae0a -> cfdbaba -> 4e166f0`
  - 这条主线基本能成立。
- `Y:\experiments\EXPERIMENT_RECORD_FULL_DATA.csv`
  - 已经足够支持“实验组 -> 研究问题”的一级映射。
- loss 主线从 Gram/Moment 转向 SWD，再叠 color / identity
  - 这一点证据很强。

## 2. 目前还不够稳的点

### 2.1 某些实验目录到底对应哪个 commit

原因：

- 很多实验目录是直接复制了 `model.py/losses.py/trainer.py`
- 但没有显式记录 commit hash

建议恢复方式：

- 逐个对实验目录中的源码计算哈希
- 与 `repo.git` 里关键提交的文件内容做近似比对

### 2.2 `c-g-w` 命名的精确定义

我目前根据代码推断它是：

- conv / global attention / window attention 混合骨架

但如果要写正式版本，最好进一步从：

- 配置命名
- `arch_ablate_*`
- `cgw/*`

中把缩写的内部含义完全坐实。

### 2.3 `style_oa`、`optuna_hpo` 与正式主线的归属

它们已经明显属于“后期联合优化阶段”，但：

- 是直接作用于最终模型
- 还是更多服务于临时分支

还可以继续深挖。

## 3. 后续可以追加的材料

### 3.1 做一份“提交 -> 结构图”对照表

建议额外产出：

- 每个关键提交一张 ASCII / Mermaid 结构图

### 3.2 做一份“实验组 -> 结论 -> 证据强度”表

列可以是：

- 实验组
- 验证问题
- 代表实验
- 最强证据
- 是否已经纳入当前主线
- 证据强度：高 / 中 / 低

### 3.3 把 `Y:\experiments` 的 summary_history 再扫一遍

这次主要利用了总表。  
如果要更深入，还可以：

- 从各目录 `summary_history.json` 重建 epoch 曲线
- 看哪些实验是“后期退化型”，哪些是“越训越好型”

## 4. 最后一句判断

这段历史最值得保留的不是“哪个目录最终分数最高”，而是：

- 这个项目如何一步步确认
  - 风格必须用分布匹配来管
  - 内容必须有单独约束
  - skip 必须控泄漏
  - color 必须低频建模
  - 复杂结构必须用系统化实验去驯服

这才是后面继续扩展模型时真正有复用价值的部分。

